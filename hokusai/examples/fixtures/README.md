# Brush fixtures

`.myb` files sourced from [mypaint-brushes](https://github.com/mypaint/mypaint-brushes)
(CC0 1.0 Universal — see the upstream repo's `COPYING`). These are unmodified copies
used as integration fixtures and as inputs to the `myb_to_png` example.
